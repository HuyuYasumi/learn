#include <ctype.h>
#include <time.h>
#include "pe12.2a.h"

#define STLEN 10
#define WLEN 80
#define TEN 41

int main(int argc, char * argv[])
{
    //int * ar;
    //int size;
    //int val;
    //scanf("%d %d", &size, &val);
    ////printf("%d %d\n", size, val);
    //ar=make_array(size, val);
    ////printf("%u\n", ar);
    //show_array(ar, size);
    //free(ar);
    make_array2();
    return 0;
}
